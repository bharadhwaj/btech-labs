#include <stdio.h>
int main(){
	int i, j, n, b[20], w = 0,w1[20],s1,s2,t=0,t1[20],o[20],temp;
	printf("Enter no.of processes: ");
	scanf("%d", &n);
	for(i = 0; i < n; i++){
		o[i] = i+1;
		printf("Enter the burst time for proc %d: ", i+1);
		for(i = 0; i < n; i++)
			for(j = 0; j < n-1; j++){
				if(
				
			
	}
}
